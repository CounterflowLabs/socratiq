"""OpenTelemetry tracer + metrics for AgentRuntime / MentorAgent / course-gen.

Importing this package has no side effects. Call ``bootstrap.init_otel()``
once at process startup to install the SDK and swap the default tracer.
"""
